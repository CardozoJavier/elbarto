import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: auto;
  padding: 0 5vw;

  .only-prev {
    margin-right: auto;
  }
  .only-next {
    margin-left: auto;
  }
`;

